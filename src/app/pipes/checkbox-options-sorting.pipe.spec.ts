import { CheckboxOptionsSortingPipe } from './checkbox-options-sorting.pipe';

describe('CheckboxOptionsSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckboxOptionsSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
